{
  "name": "@nasir/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node --enable-source-maps --import tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test ./test/bids.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@nasir/chain": "workspace:*",
    "@nasir/config": "workspace:*",
    "@nasir/db": "workspace:*",
    "@nasir/payment": "workspace:*",
    "@nasir/shared": "workspace:*",
    "fastify": "^5.1.0",
    "pino-pretty": "^13.0.0",
    "tsx": "^4.19.2",
    "viem": "^2.23.5",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
